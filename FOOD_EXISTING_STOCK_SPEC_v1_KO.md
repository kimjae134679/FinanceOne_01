# FinanceOne 기존 보유 식재료 등록 기획서 v1

## 목적

FinanceOne 사용 전에 구매해 둔 식재료를 구매일 없이 현재 재고로 등록하고, 일반 구매 재고와 동일하게 먹은 날짜와 횟수를 기록한다.

## 입력 방식

- 식비 화면에 `기존 재고 등록` 버튼을 둔다.
- 식품 종류, 상품·브랜드, 총량, 단위, 메모를 입력한다.
- 구매처와 가격은 선택 입력이다.
- 구매일 입력란은 제공하지 않으며 데이터에도 실질 구매일을 생성하지 않는다.

## 금액 규칙

- 가격을 비우면 `금액 미기록`으로 표시한다.
- 금액 미기록 재고는 식비 합계, 일·주 평균, 추이, 가격 비교에서 제외한다.
- 먹은 날짜, 먹은 횟수, 먹은 양 통계에는 포함한다.
- 나중에 수정 화면에서 가격을 입력하면 기존 계산 방식으로 다시 정산한다.

## 재고·기록 규칙

- 기존 재고도 고유 구매 ID를 가진다.
- 같은 상품을 다시 등록하거나 새로 구매해도 각각 독립 재고로 저장한다.
- `다 먹었음` 처리하면 재고 화면에서는 사라지지만 먹은 기록은 유지한다.
- Google Drive·로컬 백업에 `source`와 `priceKnown`을 포함해 PC와 모바일에서 동일하게 복원한다.

## 완료 조건

- 구매일 없는 기존 재고 추가 및 수정 가능
- 가격 없는 상태로 여러 번 먹은 기록 가능
- 가격 없는 기록이 금액 통계와 가격 비교에 섞이지 않음
- 동일 품목 재등록 시 이전 기록에 영향 없음
- PC와 모바일 동작 및 백업 데이터 구조 일치
