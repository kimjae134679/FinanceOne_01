{
  "name": "financeone-mobile",
  "version": "1.7.1",
  "private": true,
  "scripts": {
    "verify": "node scripts/verify-mobile-release.mjs && node scripts/verify-food-existing-stock.mjs",
    "sync": "npx cap sync android",
    "copy": "npx cap copy android",
    "apk:windows": "build-apk.bat",
    "apk": "cd android && ./gradlew clean assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
